# SDK Boundary Policy（模板）

> 目的：冻结公共边界，保证 Public SDK 的 API/ABI 稳定，避免应用直接耦合内部实现。

## 1. 范围与术语

- **Public SDK**：仅指 `framework/include` 对外头文件和其导出符号。
- **Internal/Core**：`service/*`、`service/stacks/*`、`feature/*` 等内部实现。
- **ABI**：二进制兼容性（符号、结构体布局、调用约定）。
- **API**：源码接口兼容性（头文件、函数签名、枚举值等）。

## 2. 边界规则（必须遵守）

1. 应用层/示例层不得 include `service/*` 私有头。
2. 新增能力优先通过 `framework/include` 暴露；禁止绕过 Public SDK。
3. Public SDK 中结构体字段禁止随意重排或删除。
4. 导出函数签名禁止做不兼容改动（参数类型、返回值、语义）。
5. 删除接口必须走“至少一个大版本”废弃周期。

## 3. 版本兼容策略

- **MAJOR**：允许不兼容变更（需迁移指南）。
- **MINOR**：仅新增能力，保持向后兼容。
- **PATCH**：仅修复缺陷，不改对外契约。

建议在头文件维护：

- `BT_SDK_VERSION_MAJOR`
- `BT_SDK_VERSION_MINOR`
- `BT_SDK_VERSION_PATCH`
- `BT_SDK_API_LEVEL`

## 4. 变更评审模板（PR 必填）

### 4.1 变更分类
- [ ] API 新增
- [ ] API 废弃
- [ ] ABI 影响
- [ ] 仅内部实现

### 4.2 兼容性说明
- 受影响接口：
- 对旧应用影响：
- 回滚策略：
- 替代路径（若废弃）：

### 4.3 验证项
- [ ] 公共头文件差异审查完成
- [ ] 导出符号差异审查完成
- [ ] 示例编译通过
- [ ] 回归测试通过

## 5. CI 检查建议

1. **Include 边界检查**：sample/app 中出现 `#include "service/` 直接失败。
2. **符号差异检查**：导出符号列表与基线比较。
3. **头文件差异检查**：Public SDK 目录变更必须带兼容性标签。
4. **版本门禁**：如果 ABI break，必须提升 MAJOR。

## 6. 发布物要求

每次 SDK 发布至少包含：

- Public 头文件包
- 导出符号清单（当前版 + 上一版对比）
- 兼容性报告（API/ABI）
- 迁移指南（如有废弃/替代）

## 7. 审计记录模板

- 发布版本：
- 审核人：
- API 变更摘要：
- ABI 变更摘要：
- 风险等级（低/中/高）：
- 结论（通过/阻塞）：
